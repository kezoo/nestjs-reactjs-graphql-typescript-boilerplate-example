import { AppConstantProps, PaginationConstants } from "../interface/app.interface"

export const GENERAL_EXAMPLE = ''
export const APP_CONS: AppConstantProps = {

}
export const PAGINATION_CONS: PaginationConstants = {
  limit: 10,
}
